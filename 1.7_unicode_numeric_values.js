let one = (symbols.codePointAt(0));
let two = (symbols.codePointAt(1));
let three = (fc1+fc2);

if ((three % 2) == 0){
	console.log(String.fromCodePoint(three));
}
else {
	console.log("Символ обнаружить не удалось");
}
